			<div class="row" align="center">

				<div class="form-group col-sm-12">
				    	    <a href="${linkTo[TenistaController].detalhaTenista(tenistaDetalhado.id,1)}" class="btn btn-primary">Informações básicas</a>
				            <a href="${linkTo[TenistaController].detalhaTenista(tenistaDetalhado.id,2)}" class="btn btn-primary">Pontuações</a>
				            <a href="${linkTo[TenistaController].detalhaTenista(tenistaDetalhado.id,3)}" class="btn btn-primary">Inscrições em torneio</a>
				            <a href="${linkTo[TenistaController].detalhaTenista(tenistaDetalhado.id,4)}" class="btn btn-primary">Torneios organizados</a>
				            <a href="${linkTo[TenistaController].detalhaTenista(tenistaDetalhado.id,5)}" class="btn btn-primary">Jogos</a>
	  			</div>
			</div>
